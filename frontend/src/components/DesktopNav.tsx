import { Button } from "./ui/button";

const DesktopNav = () => {
    return (
        <Button variant='ghost' className="font-bold hover:text-cyan-500 hover:bg-slate-400">
            Log in
        </Button>
    );
}

export default DesktopNav;
